#pragma once
#include <Runtime/Core/Core.h>

namespace Portakal
{
	/// <summary>
	/// Available flags for mesh post processing for the mesh loader
	/// </summary>
	enum class RUNTIME_API MeshProcessFlags : UInt64
	{

	};
	GENERATE_FLAGS(MeshProcessFlags, UInt64);
}
